# Timeflow 完成状态清单

> 更新时间：2026-07-28
>
> 当前分支：`vibe-coding/quick-implementation`
>
> 状态说明：本文件记录当前已经完成并提交的能力、仍未完成的能力，以及下一轮继续推进时的建议顺序。每个后续功能点仍按“完成一个功能点，提交一个 commit”的方式推进。

## 当前仓库状态

- 工作区已清理，无未提交改动。
- 本地分支当前比远端多 1 个提交。
- 最新提交：`feat(fallback): add cloud fallback policy`。
- 已有 Swagger 入口，后端启动后可访问 `/docs` 查看接口文档。

## 已完成能力

### 文档与计划

- 已整理产品说明、架构说明、MVP 计划和演示脚本。
- 已在架构文档中对齐 MS1 检查项。
- 已在演示脚本中补充 Swagger、断线重连、离线 quick add、云端兜底等说明。

### 后端基础

- FastAPI 后端骨架已建立。
- 健康检查接口已可用。
- 事项、提醒、地点、写请求、领域事件等核心模型已建立。
- Alembic / SQLAlchemy 迁移结构已存在。
- 后端测试可运行，最近一次验证结果为 `27 passed, 1 warning`。

### 事项与写入流程

- 支持手动新增事项。
- 支持通过 mock 语音命令生成候选写入请求。
- 写操作已经走 `WriteRequest` 确认门禁。
- 支持创建、修改、删除、完成事项等基础操作路径。

### 提醒能力

- 时间提醒模型和投递状态已建立。
- 支持提醒状态回传和提醒动作。
- 支持地点提醒数据模型。
- 支持进入地点、离开地点、返回地点等提醒类型的基础规则。
- 前端已有地点提醒触发检查能力。

### 重复规则

- 支持 `daily`、`weekdays`、`custom_weekdays`。
- 已校验 `time_of_day`、weekday 取值、series 状态等字段。
- 前后端都已覆盖重复规则校验。

### 同步与离线

- 已建立 WebSocket / Outbox 同步骨架。
- 前端支持 WebSocket 重连。
- 支持 `sync.request` 断线后回补事件。
- 前端已有本地缓存。
- 已实现单设备 LWW 缓存合并。
- 已实现 quick add 离线队列：离线时先本地保存 quick add，恢复网络后自动重放到后端。

### 权限与降级

- 前端已有设备权限中心。
- 通知、定位、麦克风等权限状态已有 UI 承载。
- 后端已有稳定错误码和降级路径基础。

### 云端兜底

- 已实现最小可用 `CloudFallbackPolicy`。
- 本地通知注册失败后可生成 fallback request。
- 已增加 fallback request 相关测试。

### 接口文档入口

- 前端已增加 Swagger 入口。
- 后端 FastAPI 自带 Swagger，可通过 `/docs` 查看。

## 未完成能力

### 真实语音能力

- 目前语音仍是 mock transcript / mock parser。
- 还没有接入真实录音、ASR 或 LLM 解析。
- 还需要处理录音权限、音频上传、转写失败、转写重试等真实链路。

### 真实通知与后台能力

- 本地通知能力仍需要真机验证。
- 后台通知、后台任务和系统级提醒能力还没有完整验收。
- 需要确认 Expo / 原生平台在锁屏、后台、杀进程后的实际表现。

### 真实地理围栏

- 当前地点提醒偏前台检查或手动触发检查。
- 还没有实现真正的后台 geofencing。
- 需要接入平台定位能力，并验证进入、离开、返回地点的后台触发。

### 云端兜底发送

- 当前 fallback 只会记录请求，不会真正发送短信、邮件或电话。
- `Sms` / `Email` / `Call` adapter 仍是 skeleton。
- 需要选择服务商、配置凭证、实现发送、回执和失败重试。

### 离线写入范围

- 当前离线队列只覆盖 quick add。
- 编辑事项、删除事项、完成事项、修改提醒、取消提醒等写操作还没有全部进入离线队列。
- 还需要处理离线写入冲突、失败重放、重复提交和用户可见状态。

### 多设备同步

- 当前同步策略主要是单设备 LWW。
- 多设备并发编辑、冲突提示、跨设备 cursor 管理还未完整实现。
- 需要明确冲突策略：直接 LWW、提示用户选择，或按字段合并。

### Supabase / Postgres 实联调

- 还需要用真实 Supabase Postgres 跑迁移。
- 还需要用真实数据库启动后端并做 API smoke test。
- 文档和环境变量中不应提交数据库密码，只应使用本地 `.env` 或部署环境变量。

### 前端完整验收

- 已做过类型检查和局部 lint，但最近同步、缓存、离线队列仍需要浏览器/设备手动验证。
- 需要打开前端页面逐项验证：Swagger 入口、quick add、离线恢复、WS 重连、地点提醒检查、权限中心。
- 需要处理现有全量 `npm run check` 中的格式化问题，或确认哪些是历史遗留。

### E2E 演示闭环

- 需要完整跑通演示脚本。
- 需要覆盖这些故事：
  - 语音新增日历事项。
  - 语音新增待办。
  - 到家提醒取快递。
  - 停车位置返回提醒。
  - 修改、删除、完成事项。
  - 多提醒展示。
  - WebSocket 断线后补拉。
  - 离线 quick add 恢复后同步。

## 建议下一轮顺序

1. 先启动后端并打开 Swagger，确认接口文档可访问。
2. 配置真实 Supabase 数据库连接，在本地跑迁移。
3. 对后端跑 API smoke test。
4. 启动前端，逐项验证当前已实现功能。
5. 修复前端可见问题，并每个功能点单独提交。
6. 扩展离线队列，让编辑、删除、完成等写操作也支持离线。
7. 接入真实 ASR 或先确定 ASR 服务商。
8. 接入真实通知、地理围栏和云端兜底发送。

## 暂停点

当前适合暂停。代码工作区干净，最近完成的功能点已经提交。下一轮恢复时，建议先从“启动后端 + 打开 Swagger + Supabase 迁移验证”开始，确认运行环境稳定后再继续补业务能力。
