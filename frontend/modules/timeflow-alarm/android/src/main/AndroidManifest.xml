<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

  <!-- targetSdk 30+ 的包可见性限制：不声明这条，TextToSpeech 内部查询"设备上有哪些
       TTS 引擎"会查到空列表，new TextToSpeech(...) 稳定初始化失败（status=ERROR），
       即使引擎确实装着、系统设置里试听正常也一样——真机 adb logcat 实测确认过。
       官方文档明确要求声明这条：
       https://developer.android.com/reference/android/speech/tts/TextToSpeech.Engine#INTENT_ACTION_TTS_SERVICE -->
  <queries>
    <intent>
      <action android:name="android.speech.tts.TextToSpeech.Engine.INTENT_ACTION_TTS_SERVICE"/>
    </intent>
  </queries>

  <application>
    <activity
      android:name="com.timeflow.alarm.RingActivity"
      android:exported="false"
      android:excludeFromRecents="true"
      android:showWhenLocked="true"
      android:turnScreenOn="true"
      android:taskAffinity=""
      android:theme="@android:style/Theme.DeviceDefault.NoActionBar"/>
    <receiver
      android:name="com.timeflow.alarm.AlarmReceiver"
      android:exported="false"
      android:enabled="true"/>
    <receiver
      android:name="com.timeflow.alarm.BootReceiver"
      android:exported="false"
      android:enabled="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
      </intent-filter>
    </receiver>
    <service
      android:name="com.timeflow.alarm.AlarmSoundService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback"/>
  </application>
</manifest>
